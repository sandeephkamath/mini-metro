# Test Report — 2026-07-04 — Fixed Flow Regression + Debug Overlay Visual Check

Full suite (`npm test`): 8 passed, 1 failed, 1 skipped. Same pass/fail shape as the prior run in this repo — no new regressions in the fixed flows. This run's added focus: actually opening every attached screenshot (not just reading assertions) to check the debug overlay, train presence, and event-log color coding called out in `themes/metro.md` §6 and `DEBUG.md`, plus free-form exploration of Extend-a-Line, Mid-Line-Insertion, debug-mode-mid-drag, a 3-line station cycle, and restart-spam (none of which have dedicated spec files under `testing/flows/`).

| ID | Flow | Classification | Symptom | Expected | Repro Steps | Severity |
|----|------|----------------|---------|----------|-------------|----------|
| G1 | Station Overflow / Game Over | Already Tracked — confirmed | `overflow-gameover.spec.ts` fails: phase stays `playing` after 8 alternating-destination passengers are debug-added to `circle` (capacity 6); `restart.spec.ts` self-skips as a direct consequence (its `test.skip` guard fires because phase never reaches `gameover`) | Phase becomes `gameover` once a station's queue exceeds max capacity (`core/logic.md` §3 Node Overflow); tracked in `specs/memo.md` "Bugs" as the `< maxCapacity` vs `> maxCapacity` gating bug | `testing/flows/overflow-gameover.spec.ts` — start game, enable debug mode, debug-add passengers alternating triangle/square destinations to `circle` until phase check | Blocks play (loss condition unreachable) |
| G2 | Debug Mode Toggle (and every other flow run with debug mode on) | New Finding | The canvas-drawn debug panel (`renderDebug.ts`, top-right, starting at `y=4`) visually overlaps the DOM `HUD` bar (`HUD.tsx`, `position: absolute; top: 0`, `zIndex: 10`, height ~40px). Both are semi-transparent and both render text in the same top-right pixels — the HUD's "drag between stations to draw lines" hint and the debug panel's header/speed-indicator line visibly interleave into garbled, hard-to-read text in every debug-mode screenshot taken this run | Not explicitly specified, but `DEBUG.md` describes the overlay as "a semi-transparent panel on the right side of the canvas" showing readable Train Status / Event Log sections, and `themes/metro.md` §7 requires the HUD bar (score, week) always visible during `playing` — the two are drawn by unrelated systems (DOM vs canvas) with no coordination, so neither is fully legible where they overlap | Start game, press `D` to enable debug mode, screenshot the canvas — overlap is visible immediately and persists in every subsequent frame while debug mode is on (confirmed in screenshots from `debug-mode.spec.ts`, `weekly-delivery.spec.ts`, `passenger-direct-delivery.spec.ts`, `passenger-transfer-routing.spec.ts` (both tests), and a free-form 3-line-cycle exploration) | Cosmetic (debug-only surface, doesn't block play, but actively hurts the tool's own readability) |
| G3 | Passenger origin-station boarding delay (free-form re-check) | Already Tracked — confirmed | Re-observed this run: `passenger-direct-delivery.spec.ts` took 11.2s to score, consistent with the previously-documented delay | Documented as New Finding F2 in `testing/reports/2026-07-04-passenger-routing.md` (not yet in `specs/memo.md` or the Bug Log) — root cause `createTrain` (`src/logic/trains.ts`) starting a new train `state: 'moving'` instead of `stopped`, so it doesn't attempt to board passengers already waiting at its spawn station until a full round trip completes | See `testing/reports/2026-07-04-passenger-routing.md` F2 for full repro | Cosmetic / cosmetic-adjacent |

## Confirmed correct (no findings)

- **Direct delivery, one-hop transfer, anti-bounce** (`core/logic.md` §3 rules 1-3): all three verified again via screenshots this run, including event-log entries color-coded exactly per `DEBUG.md` (white "boarded", orange "transfer → waiting", red "skipped (wrong dir)", green "delivered ✓").
- **3-line station cycle (free-form)**: built circle↔triangle, triangle↔square, and square↔circle as three separate lines (a network where every pair of stations has both a direct line and a 1-hop alternative through the third station). A passenger at circle destined for square correctly boarded the direct circle→square line rather than being routed through triangle — anti-bounce and direct-delivery checks hold up even with a fully-connected 3-station cycle.
- **Extend a Line** (`core/logic.md` §4, free-form — no dedicated spec file exists for this flow): dragging from a line's end marker (computed from the station's outward unit vector + 20px tab length) onto a new debug-added station correctly appended it to the line, single route, same color.
- **Mid-Line Insertion** (`core/logic.md` §2/§4, free-form — no dedicated spec file exists for this flow): dragging from the midpoint of an existing segment onto a new station correctly split the segment and inserted the station between its two neighbors.
- **Toggling debug mode mid-drag** (free-form): starting a line-draw drag, pressing `D` mid-drag, then releasing on the target station still completed the line correctly with no stuck drag state or crash; a subsequent clean drag (new route, parallel to the first) also worked normally afterward.
- Train presence confirmed visually after `draw-line.spec.ts` (small dark rectangle with colored border on the line, per rendering order item 9).
- Weekly Delivery screenshot (4x debug speed, ~20s wall time) showed Week bumping to 1, a "New line unlocked" toast, a 4th line color available, and new stations/passengers spawned — matches `themes/metro.md` §4.
- Boot/Start/Draw-line/Restart(-skip) flows all behave as expected.

## Notes for whoever picks this up

- G1/G3 are not new — surfaced again only because the affected flows were re-run; no new information beyond what's already in `specs/memo.md` and `testing/reports/2026-07-04-passenger-routing.md`.
- G2 (debug-overlay/HUD overlap) is genuinely new and not mentioned anywhere in `themes/metro.md` §8 Known Divergences, §9 Bug Log, or `specs/memo.md`. Worth a human decision on whether to promote it to the Bug Log (it's a real, always-reproducible visual bug in a dev-facing tool) — likely fix is either moving `renderDebug`'s panel down below the HUD bar's height (~40px) or giving the DOM HUD bar a solid (non-transparent) background so canvas content underneath doesn't bleed through.
- Two spec-table flows (`Extend a Line`, `Mid-Line Insertion`) have no dedicated file under `testing/flows/` — they were only exercised this run via throwaway scripts (written, run, then deleted per the isolation rules). Both passed; a human may want to promote them into permanent spec files for regression coverage going forward.
- No `src/` or `specs/` files were modified. All throwaway exploration scripts were removed from `testing/flows/` after use, and the `testing/report_tmp/` scratch directory used for per-flow screenshot mapping was deleted.
